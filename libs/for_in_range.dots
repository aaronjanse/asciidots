`` A is the start value
`` B is the end value
`` O is the output (with the id of A)
`` C is the output when the loop ends
``   O
`` A + B
``   C

%^X `` make sure that X doesn't conflict with anything
%$AOBC

  C
  |
B-X-A
  |
  O

  /-------{+}-\
  |        1  | O
  |        #  *-/
A->-*-----~*--/
    |     | /-\
    \-{>}-*-v C
    /--*-<--~-\
    |   [-]-\ |
    \----+--+-/
         *#1/
         \B
